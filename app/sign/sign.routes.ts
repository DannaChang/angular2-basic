import { Route } from '@angular/router';

import { SignComponent } from './sign.component';

export const SignRoutes: Route[] = [
    {
        path: 'sign',
        component: SignComponent
    }
];

